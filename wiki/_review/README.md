# _review

Holding pen for anything ambiguous found during ingest or tidy passes. Nothing is ever deleted from the knowledge base — if a file looks like junk, a duplicate, or doesn't clearly fit, it (or a note about it) is set aside here for Navakanth to decide.

Currently empty.
