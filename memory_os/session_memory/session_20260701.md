# Session Log: 2026-07-01

## [09:01] AI Filmmaking long-form + screenplay export fixes

### Actions Taken
- (no actions listed)

### Summary
Fixed four defects in the Agent OS AI Filmmaking pipeline and shipped via PR #22 (squash-merged to master, 9249e46c). (1) Novelist Swarm long-form raised 'Unknown mode: novelist' — added _MODE_ALIASES normalising novelist->novel in creative_pipeline.py. (2) Selecting 300 pages produced ~30 because LLMs under-deliver ~3x per chunk — added _CHUNK_MULTIPLIER=3 so 30/100/200/300pp -> 15/51/99/150 chunks. (3) 'charmap codec cant encode U+2192' crash — a right-arrow in a [LongForm] print broke on the Windows cp1252 console; enabled global UTF-8 stdout/stderr reconfigure in server.py + made the print ASCII-safe. (4) Screenplay DOCX/PDF dialogue was left-shifted — made HTML/DOCX/PDF consistent with the industry-standard centered dialogue column (equal 1.5in L/R indents, text left-aligned) and centered CHARACTER cue in creative_exporter.py; PDF renders from the same HTML via Playwright. Also added dev_reload.py: a zero-dependency auto-reload supervisor watching agent_os/**/*.py that restarts server.py on save and relaunches on crash — now running live. All commits passed the pyrefly pre-commit hook. Full ADLC followed: branch fix/longform-chunks-mode-encoding -> verify -> commit -> push -> PR #22 -> squash-merge -> delete branch. Open follow-up: user should visually verify exported DOCX/PDF centered dialogue in a real viewer (pdftoppm not installed locally so PDF visual preview was unavailable, but generation succeeded and DOCX geometry was verified programmatically).

---
