---
date: 2026-06-12
tags: [session, agent-os]
project: "AI-Automation"
source: "Agent OS Session End"
---

# Session: Secure public subdomain deployment for Agent OS

## Key Idea
Updated Vercel static pages to automatically use the secure read-only review API key 'agent-os-review' instead of the master API key. Configured relative routes for seamless page transitions in subdir

## Details
Updated Vercel static pages to automatically use the secure read-only review API key 'agent-os-review' instead of the master API key. Configured relative routes for seamless page transitions in subdirectory hosting (/AgentOSClaude/). Deployed, verified, and confirmed that public users can view the dashboard and 3D Neural Map in read-only mode, while the owner can authenticate with full read-write privileges.

## Action / Next Steps
- [ ] (no actions listed)
