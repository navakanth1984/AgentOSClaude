version: '3.8'

services:
  speech-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./jobs:/app/jobs
      - ./models:/app/models
      - ./qualification_runs:/app/qualification_runs
    restart: unless-stopped
